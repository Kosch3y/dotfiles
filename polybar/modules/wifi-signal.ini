[module/wifi-signal]
type = custom/script
exec = scripts/wifi-signal.jl
tail = true

format = <label>
format-prefix = "直 "
format-prefix-foreground = ${colors.foreground-alt}
format-prefix-background = ${colors.background-alt}

label = %output:5%
label-background = ${colors.background-alt}
